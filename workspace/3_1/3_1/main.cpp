#include <iostream>
#include <cmath>

using namespace std;

int main(void) {
    
    int x = (4 + 2) * 3;    // Associativity, x is memory, after calculate all in right, then it assigned to left x
    
    int y = -3; // Unary minus
    
    cout << x << endl;
    
    int z = pow(2, 3); // 2 to the power(square) of 3
    
    cout << z << endl;
    
    return 0;
}
